name: Збірка Docker‑образу (локальна)

on:
  workflow_dispatch:   # тільки вручну — секрети не потрібні

jobs:
  build:
    name: 🐳 docker build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 🔧 Збираю образ
        run: docker build -t flask-demo:ci .
