name: Прибирання артефактів

on:
  schedule:
    - cron: '15 3 * * *'   # щодня о 03:15 UTC
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: 🧹 Видаляю артефакти старші за 7 днів
        uses: c-hive/gha-remove-artifacts@v1
        with:
          age: '7 days'
